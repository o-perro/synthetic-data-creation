# Output directory for generated data files
OUTPUT_DIR=data/outputs

# Default seed for reproducible runs (override with --seed CLI flag)
DEFAULT_SEED=42

# Default volume targets (override with CLI flags)
DEFAULT_CUSTOMERS=250
DEFAULT_ACCOUNTS=450
DEFAULT_TRANSACTIONS=7500
